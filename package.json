{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "db:generate": "drizzle-kit generate:sqlite",
    "db:migrate": "wrangler d1 migrations apply demo --local",
    "db:migrate:prod": "wranger d1 migrations apply demo",
    "db:studio": "drizzle-kit studio"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.18",
    "nitro-cloudflare-dev": "^0.0.7",
    "nuxt": "^3.11.2",
    "wrangler": "^3.55.0"
  },
  "dependencies": {
    "@lucia-auth/adapter-sqlite": "^3.0.1",
    "@nuxt/ui": "^2.16.0",
    "arctic": "^1.8.1",
    "drizzle-orm": "^0.30.10",
    "lucia": "^3.2.0",
    "oslo": "^1.2.0"
  }
}
